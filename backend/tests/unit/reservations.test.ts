import {describe,expect,it} from 'vitest'; import { AppError } from '../../src/errors.js'; import { overlaps,validateSlot } from '../../src/reservations.js';
const d=(h:string)=>new Date(`2030-01-01T${h}:00.000Z`); const area={capacity:10,openingTime:'08:00',closingTime:'22:00'};
describe('reservation rules',()=>{it('detects overlap',()=>expect(overlaps(d('10:00'),d('11:00'),d('10:30'),d('12:00'))).toBe(true));it('allows adjacent slots',()=>expect(overlaps(d('10:00'),d('11:00'),d('11:00'),d('12:00'))).toBe(false));it('accepts valid slot',()=>expect(()=>validateSlot(area,{commonAreaId:'x',startAt:d('10:00'),endAt:d('11:00'),attendeeCount:5})).not.toThrow());it.each([[d('11:00'),d('10:00'),'INVALID_TIME_RANGE'],[d('07:00'),d('08:00'),'OUTSIDE_OPENING_HOURS'],[d('10:00'),d('11:00'),'CAPACITY_EXCEEDED']] as const)('rejects invalid slot', (startAt,endAt,code)=>{try{validateSlot(area,{commonAreaId:'x',startAt,endAt,attendeeCount:code==='CAPACITY_EXCEEDED'?11:1})}catch(e){expect(e).toBeInstanceOf(AppError);expect((e as AppError).code).toBe(code)}})});
